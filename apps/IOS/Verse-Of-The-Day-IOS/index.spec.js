import 'react-native-mock';
import 'react-native';
import React from 'react';
import test from 'ava';
// import IndexIOS from './index.ios';

test('has a .Foo class name', (t) => {
  t.true(typeof IndexIOS === 'object');
});
