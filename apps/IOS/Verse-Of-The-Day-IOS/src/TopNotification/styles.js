import {
  StyleSheet,
} from 'react-native';

export default StyleSheet.create({
  TopNotificationContainer: {
    alignItems: 'center',
    justifyContent: 'flex-end',
    paddingTop: 25,
    paddingBottom: 10,
    paddingLeft: 10,
    paddingRight: 10,
  },
});
