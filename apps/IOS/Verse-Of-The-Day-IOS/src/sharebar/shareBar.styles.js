import {
  StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
  buttonContainer: {
    width: 70,
  },
  button: {
    color: 'rgba(0,0,0,.8)',
  },
  shareBar: {
    height: 40,
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    marginTop: 20,
  },
});

export default styles;
